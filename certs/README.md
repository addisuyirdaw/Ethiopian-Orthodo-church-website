# TLS Certificates (generated by mkcert)

These files are generated automatically. Do NOT commit them to git.

Run once to generate:
```
mkcert -install
mkcert -key-file certs/key.pem -cert-file certs/cert.pem localhost
```
